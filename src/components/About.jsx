import React from 'react'

const About = (props) => {
    return (
        <div >
            <h3>It's a basic web app to convert text into upper and lower case and determine the length of the text and numbers of words in the text</h3>
        </div>
    )
}

export default About
